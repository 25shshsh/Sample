6/15 : 소셜로그인 토큰 받아오기.

6/16 ClubUserDetailsService의 loadUserByUsername()에서 ClubAuthMemberDTO 객체에 ClubMember의 해시함수 암호가 안 담겨서 따로 또 담아줌.
이 문제로 인해 인증실패로 로그인 안 됐었음


<!-- 현재 로그인 된 정보를 post방식으로 보내려면 -->
<input type="hidden" class= "form-control" name="writer" th:value="${#authentication.principal.username}">



<!-- 타임리프 다른 변수에 담아서 사용 -->
<div th:with="temp= ${dto.writer.email}">
<a th:text="${temp}"></a>


<!-- 현재 로그인 된 사용자와 게시글 작성자가 같을 경우에만 수정버튼이 보이게하려면 -->
<div th:if="${dto.writer.email == #authentication.principal.username}">

#8 Optional 사용
#12 이슈12 메서드 추상화 https://github.com/25shshsh/Sample/issues/12
++ mapstruct라이브러리로 대체하자. (지금은 2가지 기능 동시)